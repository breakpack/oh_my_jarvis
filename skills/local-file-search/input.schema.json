{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "local-file-search input",
  "type": "object",
  "properties": {
    "query": {
      "type": "string",
      "description": "검색할 파일명 패턴 또는 파일 내용 검색어"
    },
    "root": {
      "type": "string",
      "default": ".",
      "description": "워크스페이스 루트(WORKSPACE_ROOT 환경변수, 없으면 현재 작업 디렉터리) 기준 상대 경로. 생략하면 워크스페이스 루트 전체를 검색한다. 워크스페이스 루트를 벗어나는 경로는 거부된다."
    }
  },
  "required": ["query"],
  "additionalProperties": false
}
